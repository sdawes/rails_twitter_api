<!DOCTYPE html>
<html>
  <head>
    <title>RailsTwitterApi</title>
    <link href="//fonts.googleapis.com/css?family=Raleway:400,700" rel="stylesheet">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- <%= link_to "Sign in with Twitter", "/auth/twitter" %> -->
    <% if current_user %>
    <% twitter_client.followers("stephenrdawes").each do |follower| %>
      <%= follower.name %>, <%= follower.location %>
      <hr/>
    <% end %>
      <p>Signed in as <%= current_user.name %></p>
      <%= link_to "Sign_out", signout_path %>
    <% else %>
      <%= link_to "Sign in with Twitter", "/auth/twitter" %>

    <% end %>

    


    <%= yield %>
  </body>
</html>
